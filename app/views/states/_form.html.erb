<%= form_for(@state, :html=>{:class=>"mono-form"}) do |f| %>
  <% if @state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@state.errors.count, "error") %> prohibited this state from being saved:</h2>

      <ul>
      <% @state.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label "name" %>
    <%= f.text_field :name , :class=>"title" %>
  </p>
  <p>
    <%= f.label "abbr" %>
    <%= f.text_field :abbr , :class=>"title" %>
  </p>
  <p>
    <%= f.check_box :approved %><%= f.label "Approved?" %>
  </p>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
