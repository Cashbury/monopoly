<% title "Edit Engagement" %>

<%= render 'form' %>

<p>
  <%= link_to "Show", business_program_campaign_engagement_path(@business, @program, @campaign,@engagement) %> |
  <%= link_to "View All", business_program_campaign_engagements_path(@business, @program , @campaign) %>
</p>
