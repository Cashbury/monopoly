<% title "Edit Reward" %>

<%= form_for @reward, :url => business_program_campaign_reward_path(@business, @program, @campaign), :html => {:multipart => true, :method => :put}  do |f| %>
	<%= render :partial => 'form', :locals => {:f => f} %>
<% end %>
