<% title "Edit Engagement" %>

<%= render 'form' %>

<p>
  <%= link_to "Show", business_program_engagement_path(@business, @program, @engagement) %> |
  <%= link_to "View All", business_program_engagements_path(@business, @program) %>
</p>
